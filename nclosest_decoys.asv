function new = nclosest_decoys(hash, n)
%reads a hashmap - already sorted by key, where key = distance to forgery
%returns an array on top n filenames according to values
fnames = values(hash);
out = cell(1:n);
% new = cell(1:n);

for i=1:n
    out(i) = fnames(i);
end
length(out)
for i=1:length(out)
    temp = out(i);
    whos temp
    new(i) = char(out(i));
end
new
