function N = displacement_matrix(h1, h2)
%1 step - Displacement matrix generation 256x256 between a forgery (h1) 
%and a decoy (h2)
%Repeat until divergence < Dmax
N = zeros(256, 256);

for i=1:256
    for j=1:256
        
    end
end